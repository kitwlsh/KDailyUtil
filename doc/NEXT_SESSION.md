# 📋 다음 세션 브리핑 (2026-09-04 기준)

> **신규 세션은 이 문서 하나만 읽고 사용자에게 브리핑하면 된다.** 기술 배경은 각 항목의 링크에 있다.
> 상태: **진행 중인 코드 작업 없음.** 08-25 이후 KDailyUtil은 한 줄도 바뀌지 않았다(09-04 확인: 작업 트리 깨끗 · 단위 테스트 **20건 전부 통과** 재확인).
> ✅ 09-04에 **밀려 있던 5커밋을 푸시했다**(`157149c`) — 원격은 이제 로컬과 같다.
> ❗ **다음 행동 = vc8 빌드·업로드.** 503 수정이 **아직 사용자에게 가지 않았다.** 단 **버전 상향·release는 사용자 지시로 다음 세션으로 미뤘다**(09-04).

---

## ✅ 09-04에 드러나고 그날 해소한 것 — 08-25 작업이 푸시되지 않았다

`main`이 `origin/main`보다 **4 커밋 앞서 있었다.** 08-25 세션이 커밋만 하고 푸시하지 않았다.

| 커밋 | 내용 |
|---|---|
| `c74ef26` | 폴백 루프 테스트(기기 없이 검증) + 오늘자 API 재측정 |
| `c57b146` | 503 이식 결과 문서 반영 |
| `3f90470` | 🔴 **503 대응 본체** — 13종 AI가 전부 무방비였던 것을 고친 커밋 |
| `8b16840` | 세션 착수점 갱신 + 레버 레지스트리 오기재 정정 |

→ **503 수정이 열흘간 이 기기 로컬에만 있었다.** 다른 기기·다른 세션에서는 보이지 않고, 이 기기가 죽으면 사라질 상태였다.
→ 얄궂게도 08-25 브리핑은 K장부를 두고 *「ahead 4 — 그 세션의 몫」* 이라고 적었는데 **자기가 같은 일을 했다.** K장부 쪽은 그 사이 푸시로 해소됐다(09-04 확인 · in sync).
→ ✅ **09-04에 이 문서 갱신본(`157149c`)까지 5커밋을 푸시해 해소했다.** 남은 교훈은 §주의사항의 «커밋은 저장이 아니다» 한 줄이다.

---

## 지금 상황 한 문단

**3개 앱 전부 라이브이고 밀린 «출시»는 없다. 그러나 밀린 «반영»이 하나 있다** — KDailyUtil 503 수정이 로컬 커밋에서 멈춰 있다(위 §). 08-25 이후 열흘 동안 이 저장소는 정지 상태였고, 그 사이 **K장부 세션만 09-03에 대규모로 움직였다**(가계부·카드 12커밋). 그 작업은 전부 장부 기능이라 **KDailyUtil이 받아 갈 AI 산출물은 없다** — 08-25에 받은 503 이식이 마지막이었다.

→ **1순위 = vc8 출하**(푸시는 09-04에 끝냈다). 그다음이 «AI 체험판»(§할 일 4)이다.

| 앱 | 스토어 상태 | 다음 업로드 |
|---|---|---|
| KDailyUtil | ✅ v1.6.1(vc7) 라이브(08-12) — 🔴 **라이브에는 503 수정이 없다** | **vc8**(수정 커밋·푸시 완료 · **빌드 대기**) |
| K장부 | ✅ v1.0.2(vc3) 라이브(08-14) — 09-03 현재 **vc4 작업 진행 중**(미출시) | **vc4** (그 세션 몫) |
| KLotto645 | ✅ v1.0.3(vc13) 라이브(08-04) — 08-11 이후 커밋 없음 | **vc14** |

> ⏱ **심사 기간 실측**: 업데이트는 **당일~하루**(K장부 v1.0.2 = 업로드 당일 승인). 신규 앱 첫 심사만 4일.
> `versionCode`는 반려에도 소모되며 되돌릴 수 없다.

---

## ✅ 할 일 1 — 503(모델 과부하) 대응 이식 — **코드 수정 완료(2026-08-25, `3f90470`) · 푸시 완료(09-04) · vc8 빌드 대기**

### 쉽게 말하면
**구글 AI 서버가 붐비면(503) KDailyUtil은 「연결에 실패했습니다」만 뱉고 끝났다.** 재시도도, 다른 모델로 넘어가지도 않았다. 사용자 눈에는 *「엄청 오래 걸리다가 그냥 실패」* 로 보이고, 원인을 알 방법이 없었다.

> ✅ **2026-08-25에 고쳤다.** 아래는 무엇을 왜 고쳤는지의 기록이다. **단 스토어(vc7)에는 아직 없다 — 반영하려면 vc8 빌드·업로드가 필요하다.**

**이건 추측이 아니다.** K장부가 08-18에 **실사용 신고 → 실제 API로 재현 → 원인 확정**까지 끝냈다(커밋 `d56ff6b`). 같은 날 실측:

| 모델 | 결과 |
|---|---|
| `gemini-flash-latest` | 🔴 **503 UNAVAILABLE**(응답까지 3~70초) |
| `gemini-3.5-flash` | ✅ 18.3초 정상 |
| `gemini-3.6-flash` | ✅ 44.9초 정상 |
| `gemini-2.5-flash` | ❌ 404(신규 계정 차단 — 08-12에 고친 그 장애) |
| `gemini-2.0-flash` | ❌ **이 계정에 아예 없다**(models 목록에 없음) |

### KDailyUtil에서 확인된 것 → 전부 조치함 (2026-08-25)

[`GeminiManager.kt`](../app/src/main/java/com/kitwlshcom/kdailyutil/data/remote/GeminiManager.kt) 기준:

1. 🔴 **503이 어느 그물에도 안 걸렸다** — `isModelUnavailable()`이 404 계열만 잡아서 503은 `throw e`로 빠졌다(폴백도 재시도도 없음). `testConnection()`의 오류 분기에도 503 칸이 없어 **영문 원문이 그대로 노출**됐다.
2. 🔴 **폴백 후보에 없는 모델이 들어 있었다** — `FALLBACK_MODELS`의 마지막 칸이 `gemini-2.0-flash`였다. **이 계정에 존재하지 않는 모델**이라 폴백 한 칸이 헛돌았다.
3. 🟡 **`lastGoodModel`이 없었다** — 인스턴스 단위 `resolved`만 있어서, 기본 모델이 503으로 막힌 동안 **호출마다 그 모델을 먼저 다시 찔렀다**(503 응답까지 3~70초). 「너무 오래 걸린다」의 절반이 이것이었다.

### 실제로 한 것 (커밋 `3f90470`)
원본: `KJangbu` 저장소 커밋 `d56ff6b` (파일 3개 · 81줄). 배경·실측은 커밋 메시지와 `KJangbu/doc/OPERATIONS.md`.

- `isOverloaded()` 추가 → **503은 «다음 모델로 넘어간다»**
  - ⚠️ **429와 다르게 다뤄야 한다.** 429는 기다리면 풀리지만 **503은 그 모델 자체가 붐비는 것이라 기다려도 안 풀린다** → 폴백이 맞는 대응이다
- 오류 문구에 503 분기 → *「AI 서버가 붐빈다, 당신 사용량 문제가 아니다」*  
  - 🔴 한도 소진으로 읽히면 사용자가 «내가 많이 썼나» 하고 그냥 기다린다
- **실패 원문을 `Log.w`로 남긴다** — 번역해서 보여주는 것과 별개다. 로그에 없으면 원인 미상 오류는 영영 조사할 수 없다(이번이 그 사례다)
- `FALLBACK_MODELS`에서 `gemini-2.0-flash` 제거 → `gemini-3.6-flash`로 교체
- `lastGoodModel`(앱 전역) 도입

**추가로 한 것 — 오류 안내를 한 곳(`aiErrorMessage`)으로 모았다.**
화면마다 `${e.message}`를 그대로 붙이고 있어서 **같은 사고를 화면 수만큼 다시 겪는** 구조였다. 사용자에게 AI 오류를 보여주는 **14곳 전부**를 이 함수로 보냈다. `StockViewModel`의 429 판정도 통일했다(`RESOURCE_EXHAUSTED`를 놓치고 있었다).

**회귀 테스트 19건 신설** — ✅ `:app:testDebugUnitTest` **20건 전부 통과** · `:app:compileReleaseKotlin` 성공.
- `AiErrorMessageTest`(10건) — 문구가 아니라 **«어느 문제인지 구분하는가»** 를 고정(503↔429↔404↔키, 404 문구가 503 판정에 안 걸리는지, 폴백 후보에 없는 모델이 없는지)
- `GeminiFallbackTest`(9건) — **폴백 루프 실동작**. 503→다음 후보 / 404→다음 후보 / **429·키오류는 넘어가지 않음** / 전부 막히면 마지막 사유 / `lastGoodModel`이 다음 인스턴스에 전달 / 원격 레버 우선순위
  - 이를 위해 `askWithFallback(call)`로 **호출부를 주입 가능하게** 뺐다(기기·네트워크·키 없이 검증). `build.gradle.kts`에 `unitTests.isReturnDefaultValues = true`도 필요했다(`android.util.Log`가 «not mocked»로 던진다 — K장부가 이미 푼 함정)

> 🔬 **뮤테이션 시험으로 테스트가 진짜인지 확인했다** — `!isOverloaded(e)` 조건을 수정 전으로 되돌리자 **`GeminiFallbackTest` 4건이 정확히 실패**했고, 되돌리니 전부 통과했다. 통과만 하고 아무것도 못 잡는 테스트가 아니다.

> 🟢 **덤으로 §할 일 3의 준비가 끝났다** — KDailyUtil에 없던 **단위 테스트 기반이 생겼다**(`ExampleUnitTest` 하나뿐이었다). `aiModel` 회귀 테스트 이식은 이제 파일 하나 추가하면 된다.
> ❗ **남은 것 = vc8 빌드·업로드.** 이 수정은 아직 사용자에게 가지 않았다.

### 🔬 오늘자 실제 API 실측 (2026-08-25) — 상세는 [`AI_KEY_NOTES.md`](AI_KEY_NOTES.md) §3-1

폴백 후보를 바꾼 근거가 **K장부의 08-18 측정값**이었어서 직접 다시 쟀다(이번 사고 자체가 «목록에 없는 모델이 폴백에 박혀 있던» 문제였다).

- ✅ `gemini-3.5-flash`·`gemini-3.6-flash` 둘 다 **실재하고 정상**(1.7~3.4초) → 폴백 후보 교체가 맞았다
- ✅ `gemini-2.0-flash`는 **목록에 아예 없다**(404) → 뺀 판단이 맞았다. *08-07엔 429였는데 그 사이 사라졌다*
- 🔴 **503은 상시 장애가 아니라 간헐적이다** — 기본 모델 21회 호출 중 **4회(약 19%)만 503**. 나머지는 정상
  - **이것이 넉 달 가까이 안 잡힌 이유다.** 개발자가 시험하면 대개 통과하고, 사용자는 5번에 1번 영문 오류를 본다 → *「가끔 안 돼요」* = 재현 안 되는 신고
  - 수정 전(vc7)에는 그 19%가 **전부 하드 실패**였다. 이제는 1.7~2.9초 더 기다리고 정상 응답을 받는다
- 🟡 **무료 등급 분당 한도가 낮다** — 연속 호출 9회쯤에서 429. **§할 일 4 체험판을 총량으로만 설계하면 사용자가 한도에 먼저 걸린다**

⚠️ 쓴 키는 **K장부 개발자 키(기존 계정)**다 → **신규 계정 경로는 이걸로 검증되지 않는다**(§할 일 3-3은 그대로 남아 있다).

### 🟡 이번에 일부러 안 한 것 — 429 자동 재시도
K장부는 429에 지수 백오프 재시도를 붙였지만(`f005780`), KDailyUtil은 **`StockViewModel`이 이미 자기 백오프 루프를 갖고 있어서**(L760~) `GeminiManager`에도 넣으면 **재시도가 두 겹으로 겹친다**. 넣으려면 `StockViewModel` 쪽을 먼저 걷어내야 한다 — 별도 작업으로 남긴다.

---

## 🔴 할 일 2 — 레버 레지스트리 오기재 — **이번 세션에서 정정함(2026-08-25)**

[`family_config/README.md`](family_config/README.md) §3-1 표가 `aiModel`을 **「KDailyUtil 전용」** 이라고 적고, 본문에서 *「K장부에 aiModel이 있어도 아무 일도 일어나지 않는다」* 고 단언하고 있었다. **사실이 아니다.**

**K장부도 2026-08-07(커밋 `62284d2`)부터 같은 최상위 `aiModel` 키를 읽는다**(`FamilyRegistry.loadAiModel`). 08-12에 표를 만들 때부터 이미 틀려 있었다.

→ 그래서 **한 앱만 구하려고 `aiModel` 레버를 당길 수 없다. 당기면 두 앱의 모델이 같이 바뀐다.**
→ ✅ 표·경고문 정정 완료. 앱별로 나누고 싶으면 `aiModel.kdailyutil` 같은 **새 키를 설계**해야 한다(지금은 없다).

> 이게 왜 위험한가: 503 사고가 나면 **가장 먼저 손이 가는 게 이 레버**다. 그때 「KDailyUtil 전용」이라고 믿고 당기면 **K장부까지 같이 모델이 바뀐다** — 사고 대응 중에 사고를 하나 더 만드는 경로였다.

---

## ✅ 할 일 3 — 남은 확인 3건 (급하지 않음 · 08-12부터 이월)

1. **스토어 '데이터 보안' 칸 반영 확인** — *공유되는 데이터: 사진, 기타 사용자 제작 콘텐츠* 가 뜨는지
   - ⚠️ **웹 스토어 페이지로는 확인 불가**(JS 렌더). **Play 앱이나 기기에서** 볼 것
2. ⚠️ **`aiModel` 원격 레버 검증** — 코드에만 있고 `family.json` 어디에도 `aiModel` 키가 없다. **한 번도 당겨보지 않은 비상 레버**(08-25 재확인: 정본·번들·라이브·K장부 사본 4곳 모두 최상위 레버 키 0개)
   - 💡 **수동 확인보다 회귀 테스트 이식이 낫다** — K장부가 `FamilyRegistryConfigTest`로 **양방향 12건**을 붙였다(없으면 켜짐 / false면 실제로 꺼짐 / 한쪽을 당겨도 다른 쪽은 안 꺼짐). 근거: *「킬스위치는 사고 난 날 처음 당겨보는 물건이라 그날 안 먹히면 최악이다.」*
   - ✅ **테스트 기반은 08-25에 생겼다** — `AiErrorMessageTest` 10건이 돌고 있다(순수 함수라 `libs.json`·`isReturnDefaultValues` 없이 통과). 다만 `family.json` 파싱 테스트는 org.json이 필요하고, 그 함정 2개는 K장부가 이미 풀어놨다(`testImplementation(libs.json)`으로 android.jar의 org.json 스텁 우회 + `unitTests.isReturnDefaultValues`로 `android.util.Log` 예외 방지) → `KJangbu/doc/OPERATIONS.md` §1-1·§3-4
3. **깨끗한 계정으로 신규 설치 확인** — 08-12 장애가 *새 계정에서만* 터지던 것이라 **기존 기기 테스트로는 검증되지 않는다**(그래서 넉 달을 몰랐다)

> 💡 **다음 업로드는 vc8이다. `build.gradle.kts`는 아직 `versionCode = 7` / `versionName = "1.6.1"`이다** — 09-04에 **사용자가 「버전코드는 아직 올리지 말라 · release는 다음에」라고 지시**해서 일부러 그대로 뒀다. 빌드에 착수할 때 **vc를 8로 올리는 것부터** 하면 된다(버전명은 버그수정이므로 `1.6.2` 제안).
> 빌드 **전에** 서명 준비 확인(`local.properties`의 `release.*` 4개 + 키스토어) — `hasReleaseSigning`이 false면 **경고 한 줄 없이 서명 없는 AAB**가 나온다. ✅ 09-04 확인: `local.properties`에 `release.*` **4개 존재**. 업로드 전 `keytool -printcert -jarfile <aab>`로 서명 재확인.
> 🔴 로컬 마지막 산출물 = `app/build/outputs/bundle/release/app-release.aab` — **09-04에도 여전히 08-11 11:32짜리 = vc7, 이미 출시된 것.** 새로 빌드하지 않고 올리면 반려된다

---

## 🟢 할 일 4 — AI 체험판 (선택 · 다음 «기능» 후보 1순위)

### 쉽게 말하면
**키를 발급받지 않은 사람도 AI를 몇 번은 그냥 써볼 수 있게 하는 기능.** 지금 KDailyUtil은 키가 없으면 AI를 아예 못 쓴다.

K장부에는 **이미 만들어져 돌아가고 있다**(체험 20회 + 킬스위치). 설계 근거·약관 검토·광고/구독을 왜 안 하기로 했는지까지 정리돼 있다 → **`KJangbu/doc/AI_KEY_POLICY.md`** (정책의 단일 기준. KDailyUtil에도 그대로 적용된다)

### ⚠️ 그대로 복사하면 안 되는 이유 3가지
1. **횟수가 안 맞는다** — K장부는 AI 쓰는 곳이 사실상 영수증 인식 하나라 20회면 충분하다. KDailyUtil은 **13가지**라 한 번씩만 눌러봐도 절반이 사라진다 → **분야별 배분**(뉴스/배움터/증시). 제안: 7 / 7 / 6
2. **세는 단위** — '호출'이 아니라 '**사용자가 한 작업**' 단위. 실패는 차감하지 않는다
3. **작업 순서를 반드시 지킬 것** — `키 읽는 곳을 한 군데로 모으기 → 카운터 → 안내 → 원격 중단 스위치 → **개발자 키 넣기는 맨 마지막**`. 순서를 어겨 키부터 넣으면 **카운터 없이 전 기능이 무제한**으로 돌아간다

> KDailyUtil은 키를 읽는 코드가 **15곳에 흩어져** 있다. 한 곳으로 모으는 작업이 먼저다.
> 💡 **08-25에 오류 안내(`aiErrorMessage`)를 한 곳으로 모으는 것은 이미 했다.** 남은 건 **키를 읽는 15곳**이고, 그것이 체험판의 1단계다.

---

## ❗ 미결 — 다른 저장소 (2026-09-04 실측)

- ✅ **KDailyUtil 자신이 `ahead 4`였다 → 09-04에 푸시로 해소**(`157149c` 기준 in sync). 위 § 참조
- ✅ **`KJangbu` 미푸시 해소됨** — 08-25에 `ahead 4`였던 08-18 커밋 4건(503 수정 포함)이 푸시됐다. 현재 `main` = `origin/main`, 작업 트리 깨끗
- ✅ `k-series-config` — 깨끗·푸시 완료(08-13 `c4307fe` 이후 변동 없음)
- 🟡 `KLotto645` — 커밋은 08-11이 마지막이나 **`RENEWAL_PLAN.md`가 수정된 채 커밋 안 됨**. **그 세션 몫**(건드리지 말 것)
- ✅ **`family.json` 4곳 재확인(09-04)** — 최상위 키는 네 곳 모두 `_readme`/`_rules`/`version`/`updatedAt`/`apps`/`_reservedQueriesPackages`/`_reservedNote` 뿐. **비상 레버 키(`aiModel`·`aiTrial`·`fscApi`)는 여전히 0개 = 전부 기본값**

### 📣 K장부 근황 (08-19 ~ 09-03) — KDailyUtil이 받을 것은 **없다**

09-03에 12커밋이 한꺼번에 들어왔지만 **전부 가계부·카드·증권 기능**이다(카드 종류/결제 계좌, 카드사 6곳 이용내역 가져오기, 임의 날짜 범위·결제주기, 필터 연동 합계·정렬 6종, 스크롤 튐 수정 등). **AI·공통 인프라 변경 없음** → 이식할 것 없음. KDailyUtil이 K장부에서 받아 온 마지막 산출물은 08-25의 503 대응이다.

- ⚠️ 여전히 **스토어 vc3에는 503 수정이 없다**(K장부 vc4 미출시). 그쪽이 급하면 `aiModel` 레버를 당기게 되는데 **그 레버는 KDailyUtil도 같이 바꾼다**(§할 일 2)


## 📦 나중에 (백로그 · 착수 안 함)

| 항목 | 상태 |
|---|---|
| **증권계좌 장부(M8)** | K장부에서 거의 완료. 남은 건 미래에셋 파서 하나. **그 세션 담당** |
| **블박 신고 도우미 앱** | ⏸️ 보류. 번호판 인식 로컬 검증이 먼저 → [`SISTER_APP_DASHCAM_REPORT_PLAN.md`](SISTER_APP_DASHCAM_REPORT_PLAN.md) |
| **출근길 교통 브리핑 앱** | 다음 신규 앱 후보 중 가장 안전·저렴 → [`SISTER_APP_IDEAS_BACKLOG.md`](SISTER_APP_IDEAS_BACKLOG.md) 아이디어 A |
| **K운복(AI 운세)** | ⏸️ 보류(08-04 사용자 지시) |
| 죽은 코드 정리 | `GeminiManager.summarizeNews()` · `extractArticleContent()` 는 호출부 0. 삭제해도 무방 |

---

## ⚠️ 주의사항 (사고 방지)

- 🔴 **모델 이름을 버전으로 박지 말 것**(08-12 교훈) + **폴백 후보에 실재하지 않는 모델을 두지 말 것**(08-18 교훈). 후보를 넣을 땐 **실제 호출로 확인**한다 — ListModels 목록에 있어도 generateContent는 거부될 수 있다
- 🔴 **`aiModel` 레버는 KDailyUtil과 K장부에 동시에 적용된다** → §할 일 2. 당기기 전에 두 앱 모두에서 되는 모델인지 확인
- **개발자 기기에서만 테스트하면 안 보이는 장애가 있다** — 08-12 장애(신규 계정 404)가 그랬다. 깨끗한 계정 확인이 필요한 이유
- **KJangbu·KLotto645 저장소는 다른 세션이 작업 중이다.** 건드리기 전에 `git status`·`git log`부터. **그 앱 관련 결정은 그 세션이 단일 기준**
- **날짜를 추측하지 말 것.** 커밋 타임스탬프(`git log --date=format:...`)로 확인한다
- **`versionCode`는 업로드마다 +1, 반려에도 소모, 되돌릴 수 없다.** 현재: KDailyUtil vc7 · K장부 vc3 · KLotto645 vc13
- **`family.json`은 네 곳을 맞춘다** — 정본 `KDailyUtil/doc/family_config/family.json` + 번들 `res/raw/family.json` + 라이브 `k-series-config`(로컬 = `d:/DATA/20_Source/80_Git_HUB/k-series-config/k-series-config`) + K장부 사본
  - 🔴 **라이브를 덮어쓰기 전에 최상위 레버 키를 먼저 확인** → [`family_config/README.md`](family_config/README.md) §3-1
- **방침을 바꾸면 세 곳이 한 세트** — 원본 `doc/privacy-*.html` → 배포본 `k-series-config` → **Play Console '데이터 보안' 양식**. AI 전송앱 정답 = `공유됨`✅ / `임시 처리`❌ / `선택` / 목적 `앱 기능`
  - 배포본 복사는 줄바꿈 멱등 형태로: `sed 's/\r$//; s/$/\r/' <원본> > <배포본>` · 확인은 `diff --strip-trailing-cr`
- **새 문서를 만들면 README '문서 인덱스' 표에 등록**한다(K-시리즈 규칙)
- 서명 키는 저장소 밖 `d:/DATA/20_Source/_secrets/`. **업로드 전 서명 확인 필수**
- 🔴 **세션을 끝낼 때 `git status -sb`로 `ahead N`을 확인한다.** 08-25 세션이 커밋만 하고 푸시를 잊어 **503 수정이 열흘간 이 기기 안에만 있었다**(09-04 발견). 커밋은 저장이 아니다

---

## 📚 읽을 순서

1. **이 문서** — 무엇을 할지
2. `KJangbu` 커밋 `d56ff6b` 메시지 — §할 일 1의 근거·실측 전문(가장 먼저 읽을 것)
3. [`doc/AI_KEY_NOTES.md`](AI_KEY_NOTES.md) — 모델 실측표·v1.6.1이 고친 장애의 기술 배경
4. [`doc/family_config/README.md`](family_config/README.md) §3-1 — 레버 키 레지스트리(08-25 정정됨)
5. [`doc/PLAY_POLICY_COMPLIANCE.md`](PLAY_POLICY_COMPLIANCE.md) — 3앱 공용 정책 기준
6. [`doc/DEVELOPER_GUIDE.md`](DEVELOPER_GUIDE.md) 상단 '지금 상태' — 프로젝트 전반
7. `KJangbu/doc/AI_KEY_POLICY.md` — §할 일 4(AI 체험판)를 할 때만
