<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 시스템 스플래시용 테마 (Android 12+) -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/deep_charcoal</item>
        <item name="windowSplashScreenAnimatedIcon">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.KDailyUtil</item>
    </style>

    <!-- 앱 기본 테마 -->
    <style name="Theme.KDailyUtil" parent="android:Theme.Material.Light.NoActionBar" />
</resources>